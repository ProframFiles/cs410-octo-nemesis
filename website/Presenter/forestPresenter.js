require([/*the different .js files needed here (with path relative to entry path), in strings*/],
	function(/*the order of the above classes, with whatever corresponding name you'd like*/)){
	// Private variables and private functions


	return{
		// exposed methods don't put variables in here

	}
}